/**
 * enum جهت‌های مرتب‌سازی
 * 
 * این فایل شامل:
 * - تعریف جهت‌های مرتب‌سازی صعودی و نزولی
 * - استفاده در سیستم مرتب‌سازی DataGrid
 * 
 * نکات مهم:
 * - از enum برای type safety استفاده شده
 * - مقادیر string برای سازگاری با API
 */

/**
 * جهت‌های مرتب‌سازی برای DataGrid
 */
export enum SortDirection {
  /** مرتب‌سازی صعودی (A-Z, 1-9) */
  Ascending = "asc",
  /** مرتب‌سازی نزولی (Z-A, 9-1) */
  Descending = "desc",
}
