/**
 * enum حالت‌های ویرایش DataGrid
 * 
 * این فایل شامل:
 * - تعریف حالت‌های مختلف ویرایش داده‌ها
 * - استفاده در سیستم ویرایش DataGrid
 * 
 * نکات مهم:
 * - از enum برای type safety استفاده شده
 * - پشتیبانی از سه حالت مختلف ویرایش
 */

/**
 * حالت‌های ویرایش برای DataGrid
 */
export enum GridEditMode {
  /** ویرایش درون‌خطی (مستقیم در سلول) */
  Inline = "inline",
  /** ویرایش در پنجره popup */
  Popup = "popup",
  /** ویرایش دسته‌ای (چندین رکورد) */
  Batch = "batch",
}
