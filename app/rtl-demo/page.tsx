/**
 * صفحه نمایش RTL برای تست عملکرد
 * 
 * این صفحه شامل:
 * - نمایش کامل عملکرد RTL
 * - تست تراز متن‌ها
 * - تست دکمه‌ها و فرم‌ها
 * - تست جداول و لیست‌ها
 * - تست انیمیشن‌های RTL
 */

import { RTLDemo } from '@/components/ui/rtl-demo'

export default function RTLDemoPage() {
  return <RTLDemo />
}
